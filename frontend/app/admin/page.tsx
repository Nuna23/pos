'use client';

import AdminShell from '@/components/admin/AdminShell';

// Entered by URL only (/admin) — intentionally not linked anywhere in the UI.
export default function AdminPage() {
  return <AdminShell />;
}
